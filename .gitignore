# --- 1. ความปลอดภัย (สำคัญมาก) ---
.env
# ไฟล์นี้เก็บ API Key ห้ามหลุดออกไปเด็ดขาด

# --- 2. Virtual Environment (โปรแกรมจำลอง) ---
.venv/
venv/
env/
# เราไม่เอาตัว Environment ขึ้น เพราะแต่ละเครื่องต้องสร้างเอง

# --- 3. Python Cache (ไฟล์ขยะจากการรัน) ---
__pycache__/
*.pyc

# --- 4. ฐานข้อมูล Vector (สมอง AI) ---
db_storage/
# อันนี้เป็นไฟล์ Database ขนาดใหญ่และเปลี่ยนตลอดเวลา ไม่ควรเอาขึ้น Git
# ยกเว้นว่าคุณอยากBackup สมอง AI ด้วย ให้ลบบรรทัดนี้ออก

# --- 5. ไฟล์ System/Editor ---
.DS_Store
.vscode/
.idea/

# --- 6. (ทางเลือก) ไฟล์เอกสารที่อัปโหลด ---
# knowledge_base/
# ถ้าอยากเก็บไฟล์กฎต้นฉบับไว้ใน Git ให้ใส่ # ข้างหน้า (Comment)
# ถ้าไม่อยากเก็บไฟล์กฎ (เพราะเป็นความลับ) ให้เอา # ออก